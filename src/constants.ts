import { env } from "./env"

const BASE_PATH = './assets'

export const PIXEL_ART_PATH = `${BASE_PATH}/pixelart.json`
export const INPUT_PATH = `${BASE_PATH}/${env.INPUT_FILE_NAME}`
export const PREVIEW_PATH = `${BASE_PATH}/preview.png`